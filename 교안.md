# 웹 프론트엔드 전체 교안 모음집 (1장 1강 ~ 3장 1강)

---

# 1장: HTML을 활용한 웹 문서 구조 설계

---

## [1장 1강 : HTML의 개념과 텍스트 태그]

### 학습 목표
- 웹 표준의 개념을 이해하고 HTML 필수 구성 요소의 역할을 설명할 수 있다.
- 웹 요소의 두 가지 배치 방식(블록, 인라인)의 차이점을 설명할 수 있다.
- 제목 태그와 본문 서식 태그를 활용하여 구조적 위계가 명확한 웹 문서를 마크업할 수 있다.

### 사용 프로그램
- VS Code, 웹 브라우저(Chrome)

---

### 이번에 배울 것은?
**우리가 매일 스마트폰으로 접속하는 웹사이트 화면은 어떻게 정돈되어 보일까요?**

매일 받는 마트 영수증을 보면 맨 위에 '가게 이름'이 있고, 중간에 '구매 항목', 맨 아래에 '결제 금액'이 보기 좋게 나뉘어 있습니다. 웹페이지도 이처럼 글자와 그림을 무작정 배치하는 것이 아니라, 각각의 역할에 맞는 '뼈대'를 먼저 세워야 합니다. 이 뼈대를 만드는 언어가 바로 HTML입니다.

이번 강에서 HTML의 필수 구조를 배우고 글자에 올바른 이름표를 붙이는 방법을 알아봅니다.

---

### 실습 환경 구성
본격적인 학습을 시작하기 전, 코드를 작성하고 실행할 환경을 준비합니다.
1. 작업 폴더에 `chapter01-1`라는 이름의 새로운 폴더를 생성합니다.
2. VS Code를 실행한 후 메뉴에서 `File > Open Folder`를 선택하여 생성한 `chapter01-1` 폴더를 엽니다.
3. 탐색기 빈 곳을 우클릭하거나 새 파일 아이콘을 눌러 `index.html` 파일을 생성합니다.

---

### 1. 웹 문서의 뼈대, HTML과 웹 표준
> **HTML은 웹페이지의 뼈대를 구성하는 건축 설계도입니다.**  
> 웹 표준이라는 전 세계적인 규칙에 맞추어 작성해야 어떤 브라우저에서도 화면이 깨지지 않고 올바르게 나타납니다.

#### 1.1 마크업 언어와 웹 표준의 이해
- **마크업 언어(Markup Language)**: 태그를 사용하여 문서의 데이터가 어떤 역할(제목, 본문 등)을 하는지 표시하는 언어입니다.
- **웹 표준(Web Standards)**: 크롬, 사파리, 엣지 등 어떤 웹 브라우저에서 보아도 동일한 화면과 기능을 제공하도록 만든 국제 공식 규칙입니다.
- **태그(Tag)란?**: 옷에 붙은 가격표나 상품 정보 택(Tag)처럼, 웹페이지의 특정 글자나 그림이 어떤 의미와 역할을 가지는지 컴퓨터에게 알려주는 '이름표'입니다.
  - 일반적으로 `<태그명>`으로 시작해서 `</태그명>`으로 끝나는 쌍을 이룹니다.
  - 시작과 끝 태그 사이에 실제 화면에 보여줄 내용을 넣습니다. (예: `<p>내용</p>`)
  - **예외 사항**: 일부 태그(예: `<meta>`, `<img>` 등)는 끝나는 태그가 없는 **단독 태그** 형태로 사용됩니다.

#### 1.2 HTML 필수 구성 요소와 기본 구조
HTML 문서는 항상 정해진 4가지 필수 태그를 포함하여 시작해야 합니다.
1. `<!DOCTYPE html>`: 현재 문서가 최신 HTML5 버전으로 작성되었음을 브라우저에 알리는 선언문입니다.
2. `<html>`: 전체 HTML 문서의 시작과 끝을 지정하는 가장 바깥쪽의 그릇 태그입니다.
3. `<head>`: 웹페이지의 제목, 인코딩 설정 등 브라우저가 알아야 할 문서의 정보(메타데이터)를 담는 구역입니다.
4. `<body>`: 스마트폰이나 모니터 화면에 실제로 눈으로 보이는 모든 콘텐츠(텍스트, 이미지, 영상)가 들어가는 구역입니다.

#### 1.3 기본 구조 작성하기
```html
<!DOCTYPE html> 
<html lang="ko"> 
<head>
    <meta charset="UTF-8"> 
    <title>나의 첫 웹 문서</title> 
</head>
<body>
    화면에 글자가 나타납니다.
</body>
</html>
```
VS Code의 `index.html` 작성 화면에서 `F5 키`를 누릅니다. 이어서 `Web App(Chrome)`을 선택해 실행하면, 작성한 문서를 크롬 브라우저에서 바로 확인하실 수 있습니다.

#### 1.4 HTML 주석 사용하기
주석(Comment)은 브라우저 화면에는 나타나지 않지만, 코드 내에 남겨두는 메모입니다. 작성한 코드의 역할을 기록해 두거나, 임시로 특정 코드가 실행되지 않도록 감추고 싶을 때 사용합니다.
- 문법: `<!--` 시작해서 `-->`로 닫으며, 이 사이에 들어가는 모든 내용은 웹 브라우저가 무시합니다.
```html
<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>HTML 주석 연습하기</title>
</head>
<body>
    <h1>HTML 주석 배우기</h1>
    <p>이 문단은 웹 브라우저 화면에 정상적으로 출력됩니다.</p>
    <!--<p>주석 처리된 영역은 화면에 표시되지 않습니다.</p>-->
</body>
</html>
```

---

### 2. 텍스트의 계층을 나누는 태그
> **태그는 텍스트에 역할을 부여하여 컴퓨터가 이해할 수 있도록 돕는 이름표입니다.**  
> 정보의 중요도에 따라 알맞은 태그를 선택하여 텍스트를 감싸주어야 합니다.

#### 2.1 제목 태그(`<h1>~<h6>`)로 중요도 표현하기
- **제목 태그**: 책의 대제목, 중제목, 소제목처럼 문서의 핵심 제목을 지정할 때 사용합니다.
- **위계 구조**: `<h1>`이 가장 중요한 대제목이며, 숫자가 커질수록 중요도가 낮아지고 글자 크기가 작아집니다.
- **주의 사항**: 웹페이지 내에서 `<h1>`은 가장 핵심이 되는 주제 하나에만 사용하는 것이 권장됩니다.

#### 2.2 화면을 채우는 두 가지 방식: 블록(Block) vs 인라인(Inline)
| 구분 | 블록(Block) 요소 | 인라인(Inline) 요소 |
| :--- | :--- | :--- |
| **줄바꿈 여부** | 태그를 사용하면 **자동으로 줄바꿈**이 일어납니다. | 줄바꿈 없이 **옆으로 나란히** 배치됩니다. |
| **영역 크기** | 가로 한 줄을 전부 차지합니다. (너비 100%) | 자신이 가진 **콘텐츠(텍스트 등) 크기만큼만** 차지합니다. |
| **주요 태그** | `<h1>~<h6>`, `<p>`, `<div>`, `<ul>`, `<li>` 등 | `<span>`, `<strong>`, `<em>`, `<a>`, `<img>` 등 |

- **블록 요소의 특징**: 면적(상자)의 개념을 가집니다. 새로운 정보의 묶음이나 문단을 시작할 때 주로 쓰입니다.
- **인라인 요소의 특징**: 흐름(선)의 개념을 가집니다. 문장 내부의 특정 단어나 일부분만 꾸며줄 때 주로 쓰입니다.
- **주의**: 기본적으로 블록 요소 내부에는 블록 요소와 인라인 요소를 모두 넣을 수 있지만, 인라인 요소 내부에는 블록 요소를 넣을 수 없습니다. (예: `<p><span>텍스트</span></p>` (O) / `<span><p>텍스트</p></span>` (X))

#### 2.3 본문 서식 태그로 문장 다듬기
- `<p>`: 하나의 독립된 문단(Paragraph)을 만듭니다. 태그가 끝나면 자동으로 줄바꿈과 여백이 생깁니다.
- `<br>`: 문단 안에서 강제로 줄바꿈(Break)을 할 때 사용합니다. 끝 태그가 없는 단독 태그입니다.
- `<span>`: 줄바꿈 없이 문장 안에서 특정 글자만 따로 묶어서 스타일을 지정할 때 사용합니다.
- `<strong>`: 내용상 매우 중요한 텍스트임을 나타내며, 글자가 굵게 표시됩니다.
- `<em>`: 흐름상 강조하고 싶은 텍스트를 나타내며, 글자가 이탤릭(기울임)체로 표시됩니다.
- `<mark>`: 형광펜을 칠한 것처럼 텍스트의 배경색을 노랗게 하이라이트합니다.
- `<u>`: 텍스트 아래에 밑줄(Underline)을 긋습니다.
- `<hr>`: 내용의 흐름이 바뀔 때 분리하기 위한 수평선(Horizontal Rule)을 긋습니다. 단독 태그입니다.

#### 2.4 텍스트 태그 적용하기 실습 코드
```html
<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>텍스트 태그 연습하기</title>
</head>
<body>
    <h1>1장. 프론트엔드 개발 입문</h1>
    <h2>1강. HTML의 개념</h2>
    <p>HTML은 웹 문서의 <strong>뼈대</strong>를 담당하는 언어입니다.</p>
    <p>웹 표준을 준수하면 브라우저의 종류와 관계없이<br>동일한 화면을 볼 수 있습니다.</p>
    <hr>
    <p>내일 배울 내용은 <span style="color: blue;">하이퍼링크와 멀티미디어</span> 태그입니다.</p>
    <p>오늘 과제는 <mark>저녁 10시까지</mark> 반드시 제출해야 합니다.</p>
    <p>참고 자료는 <u>공지사항 게시판</u>에서 다운로드할 수 있습니다.</p>
</body>
</html>
```

---
---

## [1장 2강 : 하이퍼링크, 멀티미디어 및 폼 태그]

### 학습 목표
- 하이퍼링크와 멀티미디어 태그의 삽입 방식 및 필수 속성을 설명할 수 있다.
- 다양한 폼(Form) 서식 요소를 활용하여 사용자 데이터 입력 화면을 배치할 수 있다.

### 이번에 배울 것은?
쇼핑몰 상품 클릭부터 주문서 작성까지, 우리의 웹 서핑 일상은 모두 HTML 덕분입니다. 페이지 이동, 영상 시청, 회원가입 등 웹 페이지가 단순한 문서를 넘어 우리와 '상호작용'할 수 있는 이유는 오늘 배울 링크, 미디어, 폼 태그에 있습니다.

---

### 1. 하이퍼링크 태그 (`<a>`)와 경로 지정
> `<a>` 태그는 현재 페이지에서 다른 페이지로 이동하는 '통로' 역할을 합니다. 하이퍼링크(Hyperlink)를 의미하며, 반드시 어디로 이동할지 지정을 해주는 속성과 함께 사용됩니다.

#### 1.1 핵심 속성과 경로 지정 방식
- **`href` 속성**: 연결할 목적지의 주소(URL)나 파일 경로를 지정합니다.
- **`target` 속성**: 링크를 클릭했을 때 연결된 문서를 어디에 표시할지 결정합니다.
  - `_self` (기본값): 현재 창(탭)에서 링크를 엽니다.
  - `_blank`: 완전히 새로운 창이나 탭에서 링크를 엽니다.
  - `_parent`: 현재 프레임의 부모 프레임에서 링크를 엽니다.
  - `_top`: 최상위 창에서 링크를 엽니다.
- **절대 경로**: 인터넷 상의 고유한 전체 주소 (예: `https://www.google.com`)
- **상대 경로**: 현재 내 파일의 위치를 기준으로 목적지 파일의 위치를 찾는 방식
  - `./` (점 하나): 현재 파일이 있는 폴더(디렉토리) (예: `./about.html`)
  - `../` (점 두개): 현재 폴더의 한 단계 상위(부모) 폴더 (예: `../images/photo.jpg`)

#### 1.2 타겟(target) 속성 확인하기 코드
```html
<a href="https://www.wikipedia.org" target="_self">위키백과 열기 (현재 창에서 이동)</a>
<br><br>
<a href="https://www.wikipedia.org" target="_blank">위키백과 열기 (새 창에서 이동)</a>
```

---

### 2. 멀티미디어 삽입 태그 (`<img>`, `<audio>`, `<video>`)
> 웹 페이지를 시각적, 청각적으로 풍요롭게 만드는 미디어 태그입니다. 이미지, 오디오, 비디오 파일을 화면에 불러오고 제어합니다.

#### 2.1 미디어 태그의 종류와 속성
| 대상 태그 | 속성명 | 설명 |
| :--- | :--- | :--- |
| `<img>` | `src` | 표시할 이미지 파일의 절대 또는 상대 경로 |
| `<img>` | `alt` | **대체 텍스트이자 웹 접근성용 설명** (이미지 실패 시 노출) |
| `<img>` | `width` / `height` | 이미지 너비/높이를 픽셀(px) 단위로 제한 (숫자만 입력) |
| `<video>` / `<audio>` | `src` | 재생할 동영상 또는 오디오 파일의 경로 |
| `<video>` / `<audio>` | `controls` | 재생, 일시정지, 볼륨 등 제어 바 표시 |
| `<video>` / `<audio>` | `autoplay` | 페이지 로드 즉시 자동 재생 |
| `<video>` / `<audio>` | `muted` | 음소거 상태로 설정 |
| `<video>` / `<audio>` | `loop` | 재생 완료 시 처음부터 무한 반복 재생 |
| `<video>` 전용 | `poster` | 비디오 로드 전 띄워둘 썸네일 이미지 주소 |

> ⚠️ 최신 브라우저는 소음 유발 방지를 위해 `muted` 속성이 함께 선언되어야만 `autoplay`가 작동합니다.

#### 2.2 이미지와 비디오 실습 코드
```html
<hr>
<h2>2. 나의 추천 미디어</h2>
<p>
    <img src="https://picsum.photos/300/200" alt="실습용 샘플 이미지" width="300" height="200">
</p>
<video src="https://www.w3schools.com/html/mov_bbb.mp4" controls autoplay muted loop poster="https://picsum.photos/400/225" width="400">
    최신 브라우저를 이용해 주세요.
</video>
```

---

### 3. 사용자 입력 서식, 폼 태그 (`<form>`, `<input>`)
> 폼(Form) 태그는 사용자가 입력한 데이터를 수집하는 '신청서' 역할을 합니다. 로그인, 회원가입, 검색창 등 사용자가 글을 쓰고 선택하는 모든 공간에 사용됩니다.

#### 3.1 태그의 핵심 역할과 속성
- **`action`**: 입력받은 데이터를 처리할 서버 프로그램의 주소(URL). 실습 단계나 서버가 없을 때는 임시 주소 `#` 사용.
- **`method`**:
  - `get`: 서버 데이터를 **조회(Read)**할 때 사용. 데이터가 URL 뒤 쿼리스트링(`?key=value`)으로 전달됨. 서버 데이터 불변.
  - `post`: 서버 데이터를 **생성/수정/삭제(Write)**할 때 사용. 데이터를 HTTP 본문(Body)에 담아 전달.
- **보안에 대한 오해 (GET vs POST)**:
  - "POST 방식은 주소창에 안 보여서 안전하다"는 오해입니다. 암호화되지 않은 HTTP 통신에서는 둘 다 가로챌 수 있습니다. 진정한 보안은 HTTPS 암호화 통신으로 실현됩니다.
  - 결국 GET과 POST의 올바른 기준은 보안 유무가 아니라 **'서버의 상태를 변화시키는가(Write), 단순히 조회만 하는가(Read)'**입니다.

#### 3.2 데이터를 전송하는 열쇠: `name`과 `value` 속성
- **`name` (이름표)**: 서버로 전송되는 데이터 상자의 이름(Key). name이 없으면 전송 시 완전히 유실됩니다.
- **`value` (값)**: 데이터 상자에 담기는 실제 내용물(Value).
  - 직접 입력형 (`text`, `password`, `textarea` 등): 사용자가 타이핑한 글자 자체가 자동으로 value가 됨.
  - 선택형 (`radio`, `checkbox`, `<option>` 등): 사용자가 클릭만 하므로 사전에 `value="값"`을 반드시 직접 적어주어야 함.

| 구분 | `id` 속성 | `name` 속성 |
| :--- | :--- | :--- |
| **핵심 목적** | 웹 브라우저 내부에서 특정 요소를 식별 | 웹 서버로 데이터를 전송할 때 구별 |
| **중복 허용** | 한 페이지 내 유일해야 함 (중복 불가) | 동일한 name 여러 태그에 지정 가능 (예: 라디오 그룹) |
| **주요 사용처** | CSS 디자인, JS 제어, `<label>` 연결 | 웹 서버 백엔드 데이터 처리 |

#### 3.3 `<label>` 태그와 입력 요소 연결
- **목적**: 텍스트를 클릭해도 입력창에 초점(Focus)이 맞춰져 모바일 터치 및 웹 접근성이 크게 향상됨.
- **방법 1 (명시적 방법 - 권장)**: `<label for="userName">이름</label>`과 `<input id="userName">` 매핑.
- **방법 2 (암묵적 방법)**: `<label><input type="checkbox"> 동의합니다</label>`처럼 직접 감싸기.

#### 3.4 다양한 `<input>` 타입 정리
- `type="text"`, `type="password"` (비밀번호 가림)
- `type="radio"`: 단 하나만 선택 (동일한 name 그룹)
- `type="checkbox"`: 복수 선택 가능한 다중 선택 버튼
- `type="number"`: 숫자만 입력 가능 (min, max, step)
- `type="range"`: 슬라이더 바 조절
- `type="date"`, `type="time"`, `type="datetime-local"`: 달력/시간 선택 위젯
- `type="color"`: 색상표(Color Picker) 호출
- `type="email"`: 모바일 @ 자판 노출 및 유효성 검사
- `type="file"`: 파일 업로드 (`accept="image/*"`)
- `<select>` & `<option>`: 드롭다운 선택 상자 (`selected` 속성으로 기본값 지정)
- `<textarea>`: 여러 줄의 장문 입력 (`rows`, `cols`)
- **버튼 타입**:
  - `type="submit"`: 폼 안의 모든 데이터를 action 페이지로 전송 (기본값)
  - `type="reset"`: 입력한 모든 값을 초기 상태로 일괄 초기화
  - `type="button"`: 자체 기능 없는 버튼 (자바스크립트 이벤트 연결용)

#### 3.5 폼 완성 전체 코드
```html
<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>내 프로필 및 회원가입</title>
</head>
<body>
    <h1>내 프로필 및 회원가입 페이지</h1>
    <hr>
    <h2>1. 나의 추천 링크</h2>
    <p>
        <a href="https://www.wikipedia.org" target="_self">위키백과 (현재 탭에서 열기)</a> | 
        <a href="https://www.wikipedia.org" target="_blank">위키백과 (새 탭에서 열기)</a>
    </p>
    <hr>
    <h2>2. 나의 추천 미디어</h2>
    <p>
        <img src="https://picsum.photos/300/200" alt="실습용 샘플 이미지" width="300" height="200">
    </p>
    <video src="https://www.w3schools.com/html/mov_bbb.mp4" controls autoplay muted loop poster="https://picsum.photos/400/225" width="400">
        최신 브라우저를 이용해 주세요.
    </video>
    <hr>
    <h2>3. 회원가입 신청서</h2>
    <form action="#" method="post">
        <p>
            <label for="userName">이름: </label>
            <input type="text" id="userName" name="userName" placeholder="이름을 입력하세요">
        </p>
        <p>
            <label for="userPw">비밀번호: </label>
            <input type="password" id="userPw" name="userPw" placeholder="비밀번호를 입력하세요">
        </p>
        <p>
            <span>성별: </span>
            <input type="radio" id="male" name="gender" value="m"> <label for="male">남성</label>
            <input type="radio" id="female" name="gender" value="f"> <label for="female">여성</label>
        </p>
        <p>
            <span>관심 취미(중복 가능): </span>
            <input type="checkbox" id="hobby1" name="hobby" value="sports"> <label for="hobby1">운동</label>
            <input type="checkbox" id="hobby2" name="hobby" value="reading"> <label for="hobby2">독서</label>
            <input type="checkbox" id="hobby3" name="hobby" value="coding"> <label for="hobby3">코딩</label>
        </p>
        <p>
            <label>
                <input type="checkbox" id="agree" name="agree" value="y"> 약관 및 개인정보 처리에 동의합니다.
            </label>
        </p>
        <p>
            <label for="emailDomain">이메일 계정: </label>
            <input type="text" name="emailId" placeholder="이메일 주소"> @ 
            <select id="emailDomain" name="emailDomain">
                <option value="naver.com">naver.com</option>
                <option value="gmail.com" selected>gmail.com</option>
                <option value="daum.net">daum.net</option>
            </select>
        </p>
        <p>
            <label for="intro">자기소개: </label><br>
            <textarea id="intro" name="intro" rows="4" cols="40" placeholder="자유롭게 나를 표현해 보세요."></textarea>
        </p>
        <p>
            <label for="birthDate">생년월일: </label>
            <input type="date" id="birthDate" name="birthDate">
        </p>
        <p>
            <label for="skillLevel">코딩 자신감 (1~10): </label>
            <input type="range" id="skillLevel" name="skillLevel" min="1" max="10" step="1">
        </p>
        <p>
            <label for="userAge">나이: </label>
            <input type="number" id="userAge" name="userAge" min="1" max="150">
        </p>
        <p>
            <label for="favoriteColor">프로필 테마 색상 선택: </label>
            <input type="color" id="favoriteColor" name="favoriteColor" value="#ff0000">
        </p>
        <p>
            <label for="profileFile">프로필 사진 등록: </label>
            <input type="file" id="profileFile" name="profileFile" accept="image/*">
        </p>
        <p>
            <button type="submit">가입하기</button>
            <button type="reset">다시 쓰기</button>
        </p>
    </form>
</body>
</html>
```

---

### 4. HTML 추가 태그 레퍼런스
- **리스트 태그 계열**:
  - `<ul>` (순서 없는 불릿 목록), `<ol>` (순서 있는 숫자 목록), `<li>` (개별 항목)
  - `<dl>` (설명 목록), `<dt>` (용어 제목), `<dd>` (상세 설명)
- **테이블(Table) 태그 계열**:
  - `<table>` (표 컨테이너), `<thead>` (상단 제목 행 그룹), `<tbody>` (본문 데이터 그룹), `<tfoot>` (하단 요약 그룹)
  - `<tr>` (가로 행), `<th>` (열 제목 셀, 굵은 글씨 및 중앙 정렬), `<td>` (데이터 셀)
- **포매팅 및 기능성 태그**:
  - `<small>`: 부가 설명 및 저작권 표기 (Copyright ⓒ)
  - `<sub>`: 아래 첨자 (화학식 $H_2O$) / `<sup>`: 위 첨자 (제곱 $2^{10}$)
  - `<del>`: 취소선 (할인 전 가격) / `<ins>`: 밑줄 추가 텍스트 (할인 후 새 가격)
  - `<details>` & `<summary>`: 아코디언 토글 박스 (FAQ 등 클릭 시 열고 닫힘)
  - `<blockquote>`: 외부 출처 인용 블록 (좌우 여백)
  - `<pre>`: 공백, 줄바꿈, 들여쓰기 원형 그대로 브라우저에 출력
  - `<code>`: 모노스페이스 인라인 코드 글꼴 표현

---
---

## [1장 3강 : HTML5 시멘틱 태그의 구조화]

### 학습 목표
- 시멘틱 마크업의 개념과 검색엔진 및 웹 접근성 향상 측면의 필요성을 설명할 수 있다.
- HTML5 레이아웃 태그를 활용하여 영역이 명확히 분할된 웹 페이지를 설계할 수 있다.
- `<div>`와 시멘틱 태그의 차이를 이해하고 사용 기준을 명확히 구분하여 적용할 수 있다.

---

### 1. HTML5 시멘틱 태그
> **시멘틱 태그(Semantic Tag)란 태그 자체에 '의미(Semantic)'를 담아 웹 문서의 구조를 정의하는 태그입니다.**  
> 과거에는 구역을 나누기 위해 이름 없는 상자(`<div>`)만 가득 채웠다면, 이제는 "여기는 헤더 영역이야", "여기는 푸터 영역이야"라고 명확한 이름표가 붙은 상자를 사용합니다.

#### 1.1 시멘틱 마크업 도입 배경
- **과거 HTML4**: 무의미한 `<div>` 태그에 `id="header"`나 `class="footer"`를 임의로 붙여 사용. 개발자마다 이름 규칙이 제각각이라 검색엔진이 핵심 정보 위치를 파악하기 어려웠음.
- **HTML5 해결책**: 2014년 HTML5가 표준으로 등장하면서 모든 개발자가 약속된 공통 표준 **시멘틱 태그**들을 정의하여 도입.

#### 1.2 시멘틱 태그가 필요한 3대 이유
1. **검색엔진 최적화 (SEO)**: 구글, 네이버의 검색 로봇이 탐색할 때 `<main>`이나 `<article>` 같은 태그를 인식하여 핵심 정보를 우선적으로 분석, 검색 노출에 유리함.
2. **웹 접근성 (Web Accessibility)**: 시각 장애인이 화면 낭독기(스크린 리더)를 사용할 때 "메뉴 영역입니다", "본문 영역입니다"라고 정확히 소리 내어 안내하여 차별 없는 웹 환경 제공.
3. **코드의 가독성**: 개발자 협업 시 수많은 `<div>` 숲에서 길을 잃지 않고 태그 이름만 봐도 구역의 역할을 즉시 파악 가능.

---

### 2. 핵심 레이아웃 시멘틱 태그
| 태그명 | 역할 (Description) | 사용 예시 (비유) |
| :--- | :--- | :--- |
| `<header>` | 웹사이트의 맨 위 상단 영역 (로고, 검색창, 주요 내비게이션 포함) | 신문의 제호와 발행일이 적힌 머리글 영역 |
| `<nav>` | 다른 페이지로 이동하는 링크들의 모음 (메뉴바) | 책의 맨 앞에 있는 '목차(Index)' |
| `<main>` | **웹 문서에서 단 한 번만 사용할 수 있는 핵심 본문 영역** | 잡지나 책의 '진짜 내용이 시작되는 본문' |
| `<section>` | 본문 안에서 주제별로 연관된 내용을 묶어주는 영역 | 책의 '제1장', '제2장' 같은 큰 장(Chapter) |
| `<article>` | 독립적으로 떼어내어 다른 곳에 붙여도 완전히 이해되는 단독 콘텐츠 | 신문의 독자적인 '기사 하나' 또는 블로그 '포스트 하나' |
| `<aside>` | 주요 흐름과 직접 상관없는 주변부 내용 (사이드바, 광고) | 책 옆면의 '토막 상식'이나 광고 |
| `<footer>` | 웹사이트 맨 아래 하단 영역 (회사 정보, 저작권, 약관 링크) | 책 맨 뒷장의 '발행처 정보 및 판권' |

#### 2.1 비시멘틱 태그 `<div>`의 올바른 사용 기준
- **시멘틱 태그 (`<header>`, `<main>` 등)**: 구역 안에 들어가는 내용이 의미상 독립된 콘텐츠이거나 고유 역할을 수행할 때 사용.
- **비시멘틱 태그 (`<div>`, `<span>`)**: 검색엔진에게 정보를 줄 필요가 없으며, **단지 CSS 스타일(정렬, 배경색 입히기, 박스 묶음)을 적용하기 위해 단순히 묶어주는 역할만 할 때 사용**.

#### 2.2 시멘틱 레이아웃 예제 코드
```html
<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>하루 5분 IT 뉴스 사이트</title>
</head>
<body>
    <header>
        <h1>📰 하루 5분 IT 뉴스</h1>
        <nav>
            <ul>
                <li><a href="#home">홈</a></li>
                <li><a href="#tech">테크 소식</a></li>
                <li><a href="#opinion">칼럼</a></li>
            </ul>
        </nav>
    </header>
    <hr>
    <main>
        <section id="tech">
            <h2>🔥 실시간 테크 뉴스</h2>
            <article>
                <h3>인공지능(AI)이 바꾸는 미래 대학 생활</h3>
                <p>최근 생성형 AI 기술의 발전으로 대학생들의 학습 방식이 혁신적으로 변하고 있습니다...</p>
            </article>
            <article>
                <h3>HTML5 시멘틱 마크업이 중요한 진짜 이유</h3>
                <p>시멘틱 태그를 활용한 마크업은 웹 사이트의 검색 노출 빈도를 극적으로 높여줍니다...</p>
            </article>
        </section>
        <hr>
        <aside>
            <h4>광고 및 이벤트</h4>
            <p>💻 코딩 입문자 필수 코스! 선착순 50% 할인 혜택을 놓치지 마세요!</p>
        </aside>
    </main>
    <hr>
    <footer>
        <p>&copy; 2026 하루 5분 IT 뉴스 Corp. All rights reserved.</p>
        <p>문의사항: contact@itnews5.com</p>
    </footer>
</body>
</html>
```

---
---

# 2장 : CSS를 활용한 웹 스타일링 및 기본 레이아웃

---

## [2장 1강 : CSS 기초와 스타일링 문법]

### 학습 목표
- CSS 기본 구문(선택자·속성·값)의 개념과 역할을 정의할 수 있다.
- 다양한 선택자(기본·복합·가상 클래스·가상 요소)의 구조와 차이점을 설명할 수 있다.
- 고급 선택자를 활용하여 HTML 구조 변경 없이 디자인을 제어할 수 있다.
- CSS 결합 3가지 방식(인라인·내부·외부)의 특징과 차이점을 설명할 수 있다.

---

### 1. CSS 기본 개념과 문법 구조
> **CSS(Cascading Style Sheets)**는 HTML 문서 요소들의 시각적 스타일을 지정하는 스타일시트 언어입니다.

#### 1.1 기본 문법 구조
```css
선택자 {
  속성: 값;
  속성: 값;
}
```
- **선택자 (Selector)**: 스타일 규칙을 적용할 HTML 요소를 지목합니다. (예: `h1`, `p`)
- **속성 (Property)**: 변경하고자 하는 스타일 특성을 정의합니다. (예: `color`, `font-size`)
- **값 (Value)**: 속성에 부여할 최종 수치나 형식입니다. (예: `blue`, `20px`)
- **주석**: `/* 주석 내용 */`
- **캐스케이딩(Cascading)**: 폭포수처럼 스타일 규칙이 위에서 아래로 흐르며 우선순위에 따라 최종 스타일 결정.
- **스타일 상속(Inheritance)**: 부모 요소에 적용된 텍스트 스타일이 자식 요소에게 물려 내려오는 현상 (예: `body`에 폰트 지정 시 하위 태그 자동 적용).

#### 1.2 스타일 적용 3가지 방식 비교
| 적용 방식 | 작성 위치 | 장점 | 단점 |
| :--- | :--- | :--- | :--- |
| **인라인 스타일** | 개별 태그의 `style="..."` | 빠르고 직관적 | 가독성 저하, 일괄 수정 불가 |
| **내부 스타일 시트** | `<head>` 내부 `<style>` | 외부 파일 없이 한 문서에서 관리 | 다른 HTML 문서에서 재사용 불가 |
| **외부 스타일 시트** | 분리된 독립 `.css` 파일 (`<link>`) | **구조와 디자인의 완전 분리, 유지보수 극대화 (실무 표준)** | `<link>` 태그로 정확히 연결해야 함 |

---

### 2. CSS 선택자 (Selector)

#### 2.1 기본 선택자
- **전체 선택자 (`*`)**: 문서 내 모든 요소 선택 (`* { margin: 0; padding: 0; }`)
- **태그 선택자 (`태그명`)**: 해당 태그 전체 선택 (`p { color: gray; }`)
- **클래스 선택자 (`.클래스명`)**: `class="..."` 속성을 가진 그룹 선택 (재사용 가능)
- **아이디 선택자 (`#아이디명`)**: `id="..."` 속성을 가진 유일한 요소 선택 (페이지 내 중복 불가)

#### 2.2 복합 관계 선택자 (Combinators)
- **자식 선택자 (`부모 > 자식`)**: 부모의 직계 1촌 자식만 선택
- **하위(자손) 선택자 (`조상 자손`)**: 공백 기준, 깊이 상관없이 모든 후손 선택
- **인접 형제 선택자 (`형 + 동생`)**: 특정 요소 바로 뒤에 인접한 동생 딱 하나 선택
- **일반 형제 선택자 (`형 ~ 동생`)**: 특정 요소 뒤에 오는 모든 형제 선택

#### 2.3 가상 클래스(`:`)와 가상 요소(`::`)
- **가상 클래스 (Pseudo-class, `:` 1개)**: 요소의 특정 '상태'나 '위치 조건' 제어
  - 위치/구조: `:first-child`, `:last-child`, `:nth-child(n)`, `:first-of-type`, `:nth-of-type(n)`
  - 상태: `:hover` (마우스 오버), `:active` (클릭 중), `:focus` (포커스 획득), `:checked` (체크박스 활성화)
  - 조건: `:not(선택자)` (부정), `:is(선택자1, 선택자2)` (그룹 묶음)
- **가상 요소 (Pseudo-element, `::` 2개)**: HTML에 없는 가상 요소를 CSS로 동적 생성
  - `::before`, `::after`: 요소 앞/뒤에 가상 자식 생성 (반드시 `content: ""` 속성 필요)
  - `::placeholder`: 입력창 가이드 텍스트 스타일
  - `::selection`: 드래그 선택 영역 서식

---

### 3. 스타일 적용 우선순위 (명시도 가중치)
규칙 충돌 시 다음 가중치 점수에 따라 최우선 적용됩니다:
> **인라인 스타일 (1000점) > ID (#, 100점) > 클래스/가상클래스 (., 10점) > 태그 (1점) > 전체 선택자 (*, 0점)**  
> *가중치가 같을 경우 코드상 가장 마지막(아래)에 작성된 스타일이 덮어씀 (캐스케이딩).*

---

### 4. CSS 기본 속성 다지기

#### 4.1 텍스트 및 폰트 속성
- **`font-family`**: 화면에 표시할 글꼴 지정. 시스템 폰트가 없을 때를 대비해 쉼표로 대체 폰트(Fallback Font)를 지정하며 마지막엔 일반 서체 군(`sans-serif`)을 선언.
  - 웹 폰트 연동: Google Fonts CDN `<link>` 또는 `@import`, 혹은 직접 호스팅하는 `@font-face` 방식.
- **`font-size`**:
  - `px`: 고정된 절대 픽셀 단위
  - `rem`: 최상위 `<html>` 폰트 크기(기본 16px) 기준 비례하는 상대 단위 (반응형 권장)
  - `em`: 부모 요소 폰트 크기 기준 비례
- **`font-weight`**: 글자 굵기 (normal 400, bold 700, 100~900)
- **`line-height`**: 줄 간격(행간). **단위 없는 숫자(1.5 ~ 1.6)** 사용을 실무에서 강력 권장. (단위가 없어야 자식에게 상속될 때 자식 글자 크기에 맞춰 유연하게 배수가 계산되어 글자 겹침 방지).
- **`text-align`**: 텍스트 가로 정렬 (`left`, `right`, `center`, `justify`). 정렬할 인라인 요소가 아니라 **부모 블록 요소에 선언해야 동작함**.
- **`color`**: 색상 지정 (키워드, 16진수 HEX `#RRGGBB`, `rgb()`, `rgba(R,G,B,A)`의 반투명 알파 채널).

#### 4.2 배경(Background) 관련 속성
- `background-color`: 배경 단색 지정
- `background-image: url('경로')`: 배경 이미지 지정
- `background-repeat`: `no-repeat` (반복 없음), `repeat` (바둑판 반복)
- `background-position`: 시작 기준점 (`center`, `top right`, `50% 100%`)
- `background-size`:
  - `cover`: 비율 유지하며 요소를 빈틈없이 꽉 채움 (일부 잘림 발생 가능, 반응형 배너 필수)
  - `contain`: 비율 유지하며 이미지가 온전히 다 보이도록 맞춤 (여백 발생 가능)
- `background-attachment`: `scroll` (함께 스크롤), `fixed` (뷰포트에 고정되어 패럴랙스 효과)
- **단축 속성**: `background: color image repeat attachment position / size;`

---
---

## [2장 2강 : CSS 박스 모델 및 위치 지정]

### 학습 목표
- 블록과 인라인 요소의 특징을 이해하고 명확히 구분할 수 있다.
- `box-sizing` 속성(`content-box`, `border-box`)의 차이를 정의하고 레이아웃에 적용할 수 있다.
- `display: none`과 `visibility: hidden`의 공간 차지 차이를 비교하여 설명할 수 있다.
- 박스 속성과 `position`을 활용하여 요소를 원하는 위치에 정밀하게 배치할 수 있다.

---

### 1. CSS 박스 모델의 이해
> 웹 페이지의 모든 요소는 사각형의 '박스(Box)' 형태를 가집니다.

#### 1.1 박스 모델 4대 요소
1. **콘텐츠(Content)**: 텍스트나 이미지 등 실제 내용물이 표시되는 영역
2. **패딩(Padding)**: 콘텐츠와 테두리 사이의 안쪽 여백
3. **테두리(Border)**: 요소를 감싸는 외곽 테두리선
4. **마진(Margin)**: 테두리 바깥의 바깥쪽 여백 (다른 요소와의 간격)

#### 1.2 박스 크기 계산의 기준: `box-sizing` (핵심!)
- **`content-box` (기본값)**: `width`와 `height`를 콘텐츠 영역에만 적용. 패딩과 테두리가 추가되면 박스 실제 크기가 커져 레이아웃이 깨짐. (실제 가로폭 = width + 좌우 패딩 + 좌우 테두리)
- **`border-box` (실무 필수 권장)**: `width`와 `height` 안에 패딩과 테두리가 자동으로 포함됨! 설정한 크기가 그대로 유지되어 레이아웃 계산이 매우 직관적임.
```css
* {
    box-sizing: border-box; /* 실무에서 무조건 선언하는 표준 설정 */
}
```

---

### 2. `display` 속성을 활용한 요소 유형 변경
- **Block**: 가로 한 줄 100% 차지, 줄바꿈 발생, 너비/높이/상하마진 조작 가능 (`div`, `p`)
- **Inline**: 콘텐츠 크기만 차지, 줄바꿈 없음, 너비/높이 지정 불가, **상하 마진 무시됨** (`span`, `a`)
- **`inline-block`**: 나란히 배치되는 인라인의 특성을 가지면서도, 블록처럼 너비/높이/상하마진을 가질 수 있는 하이브리드 속성 (버튼, 뱃지 제작 시 필수).
- **`display: none` vs `visibility: hidden` 차이**:
  - `display: none`: 요소를 완전히 삭제한 것처럼 **공간 자체를 소멸**시킴. 뒤의 요소가 당겨짐.
  - `visibility: hidden`: 요소의 모습만 투명하게 바꿈. **기존 물리적 공간은 그대로 유지**됨.

---

### 3. `position` 속성을 활용한 공간 배치
- **`static` (기본값)**: HTML 문서의 자연스러운 흐름 순서대로 배치. (top, left 무시)
- **`relative` (상대 배치)**: 자신이 원래 있어야 할 위치 기준 상대 이동. **`absolute` 자식 요소의 기준점(Anchor)** 역할을 수행.
- **`absolute` (절대 배치)**: 일반 흐름에서 벗어나, **`static`이 아닌 가장 가까운 부모(`relative`)를 기준으로 절대 좌표** 이동.
- **`fixed` (화면 고정 배치)**: 부모와 무관하게 **브라우저 창(Viewport)**을 기준으로 항상 화면 특정 위치에 고정됨 (스크롤 무관).

```css
/* relative & absolute 연동 예시 */
.parent {
    position: relative; /* 자식의 기준점이 됨 */
    width: 250px;
    height: 300px;
}
.child {
    position: absolute; /* 기준점 안에서 절대 좌표로 이동 */
    top: 15px;
    right: 15px;
}
```

---
---

# 3장 : 고급 CSS 레이아웃 및 반응형 웹 디자인

---

## [3장 1강 : 플렉스 박스와 그리드 레이아웃]

### 학습 목표
- Flexbox 핵심 속성(방향·정렬·비율)을 조절하여 1차원 화면을 설계할 수 있다.
- Grid 핵심 속성(구조·여백·공간)을 사용하여 2차원 바둑판 레이아웃을 구현할 수 있다.
- 화면 크기 변화에 유연하게 반응하여 늘어나고 줄어드는 가변 요소를 작성할 수 있다.

---

### 1. Flexbox (1차원 정렬)
> 요소를 가로 또는 세로라는 단일 방향(1차원)으로 유연하게 배치하는 레이아웃 시스템입니다. 부모에게 `display: flex;`를 선언하여 시작합니다.

#### 1.1 `flex-direction` (정렬 방향 설정)
- `row` (기본값): 왼쪽에서 오른쪽으로 가로 배치
- `row-reverse`: 오른쪽에서 왼쪽으로 가로 배치
- `column`: 위에서 아래로 세로 배치
- `column-reverse`: 아래에서 위로 세로 배치

#### 1.2 `justify-content` (주축 가로 정렬 제어)
- `flex-start` (기본값): 시작점(왼쪽)에 붙임
- `flex-end`: 끝점(오른쪽)에 붙임
- `center`: 중앙에 모아서 배치
- `space-between`: 양 끝 자식을 벽에 붙인 후, 남은 빈 공간을 일정하게 나누어 배치
- `space-around`: 자식 상자들의 좌우에 동일한 여백 분배 (양 끝 여백은 중간 여백의 절반)
- `space-evenly`: 양 끝 벽면과 자식 상자 사이의 모든 여백을 완전히 똑같이 분배

#### 1.3 `align-items` (교차축 세로 정렬 제어)
- `stretch` (기본값): 자식 요소를 부모의 높이만큼 위아래로 늘림
- `center`: 세로 높이 기준 정중앙 정렬
- `flex-start`: 위쪽에 붙여 정렬
- `flex-end`: 아래쪽에 붙여 정렬
- `baseline`: 텍스트 밑줄 기준선에 맞춰 정렬

#### 1.4 `flex-wrap` (줄 바꿈 처리)
- `nowrap` (기본값): 자식 상자가 찌그러지더라도 무조건 한 줄에 구겨 넣음
- `wrap`: 공간이 부족하면 자식 요소들을 다음 아랫줄로 자연스럽게 내려보냄
- `wrap-reverse`: 반대 방향 윗줄로 올려보냄

#### 1.5 `gap` 속성
- 복잡한 마진 계산 없이 Flex/Grid 자식 요소 사이의 간격을 한 번에 설정 (`gap: 20px;`)

#### 1.6 자식 요소 개별 속성 (`flex-grow`, `flex-shrink`, `flex-basis`)
- `flex-basis`: 요소의 기본 시작 크기 (`auto`, `px` 등)
- `flex-grow`: 남는 여백을 나누어 가질 확장 비율 (`flex-grow: 1`)
- `flex-shrink`: 공간 부족 시 찌그러질 축소 비율 (`0` 지정 시 절대 찌그러지지 않음)

---

### 2. CSS Grid (2차원 격자 바둑판 레이아웃)
> 가로(행)와 세로(열)를 격자 모양으로 배치하는 바둑판 형태의 2차원 레이아웃입니다.

#### 2.1 주요 속성
- `grid-template-columns`: 세로 열 칸의 개수와 크기 설계
  - `repeat(3, 1fr)`: 동일한 크기의 3칸을 1:1:1 비율로 자동 생성
  - `150px 1fr 2fr`: 첫 칸은 고정 150px, 나머지는 1:2 비율 배분
- `gap`: 격자 칸 사이의 공백 (`gap: 30px 10px;` 행 30px, 열 10px)
- `grid-column: span 2;`: 가로로 2칸 합치기
- `grid-row: span 2;`: 세로로 2칸 합치기
- `place-items: center;`: 격자 칸 내부에서 가로세로 중앙 밀착 정렬

---
---

# 🎤 부록: 상하농원 클론 코딩 연계 5분 실전 발표 대본

### [00:00 ~ 00:30] 도입
"안녕하세요. 1장부터 3장 1강까지 배운 웹 표준 기술을 바탕으로 **'상하농원 소개 페이지'**를 직접 마크업하고 스타일링한 발표자 OOO입니다.  
수업 시간에 배운 **시맨틱 구조화**, **박스 모델과 위치 지정**, 그리고 **Flexbox를 통한 1차원 정렬**을 실제 코드에 어떻게 적용했는지 중심으로 발표하겠습니다."

### [00:30 ~ 01:45] 1장: HTML 구조 설계
"먼저 **1장 HTML 구조 설계**입니다.  
**[1강]** 마트 영수증에 가게 이름, 항목, 결제액이 정돈되어 있듯 `<!DOCTYPE html>` 선언부터 `<head>`, `<body>`까지 필수 4대 구조를 지켰습니다. 가로를 다 채우는 블록 요소(`<h2>`, `<p>`) 안에 흐름을 담당하는 인라인 요소(`<span>`, `<br>`)를 올바른 중첩 규칙으로 감쌌습니다.  
**[2강]** 메뉴와 푸터는 `<a>` 태그로 링크 통로를 열었고, `<img>` 태그는 상대 경로(`images/...`)를 지정했습니다. 특히 시각장애인 스크린 리더를 위해 **모든 이미지에 `alt` 대체 텍스트를 필수 지정하여 웹 접근성을 확보**했습니다.  
**[3강]** 과거의 `<div>` 도배를 지양하고, **SEO(검색 최적화)**와 **웹 접근성**, **가독성**을 위해 `<header>`, `<nav>`, `<footer>` 시맨틱 태그를 적용했습니다. 교안 기준대로, 의미 있는 영역은 시맨틱 태그를 쓰고 **오직 CSS 스타일링 묶음용으로만 `<div>`를 사용**했습니다."

### [01:45 ~ 03:15] 2장: CSS 스타일링 및 박스 모델
"다음은 **2장 CSS 스타일링과 박스 모델**입니다.  
**[1강]** 유지보수를 위해 구조와 디자인을 분리하는 **외부 스타일 시트(`style.css`)** 방식을 채택했습니다. `body`에 폰트를 선언해 하위 요소로의 **상속**과 **폴백 폰트(`Malgun Gothic, sans-serif`)**를 적용했고, 배너에는 `background-size: cover;`와 `rgba()` 반투명 오버레이를 적용했습니다.  
**[2강]** 가장 중요한 박스 모델입니다. 기본값인 `content-box`는 패딩과 테두리가 더해지면 상자가 커져 레이아웃이 깨지므로, **전체 선택자(`*`)에 `box-sizing: border-box`를 선언**하여 패딩과 테두리가 너비 안에 쏙 포함되도록 설계했습니다.  
장바구니 뱃지는 인라인 요소를 **`display: inline-block`**으로 전환해 원형 뱃지 크기를 주었고, 배너 컨테이너에 `position: relative` 기준점을 잡은 뒤 어두운 딤 레이어에 `position: absolute; inset: 0;`을 선언해 겹쳐지는 오버레이를 구현했습니다."

### [03:15 ~ 04:30] 3장 1강: Flexbox 레이아웃
"마지막으로 **3장 1강 Flexbox를 통한 1차원 정렬**입니다.  
1. **상단 네비게이션**: `display: flex;`와 `flex-direction: row` 상태에서 **`justify-content: space-between`**으로 로고와 메뉴를 양 끝 벽면에 정렬했습니다.  
2. **'짓다·놀다·먹다' 3단 카드**: `justify-content: center;`로 가운데 모으고, 마진 계산 없이 **`gap: 20px;`**만으로 카드 사이의 간격을 균일하게 띄웠습니다.  
3. **푸터**: **`flex-wrap: wrap;`**을 적용하여 화면이 좁아지더라도 내용물이 찌그러지지 않고 자연스럽게 다음 줄로 내려가도록 유연성을 확보했습니다."

### [04:30 ~ 05:00] 마무리
"1장에서 **시맨틱 마크업으로 뼈대**를 세우고, 2장에서 **border-box와 position으로 박스 공간**을 다듬은 뒤, 3장에서 **Flexbox로 조화로운 정렬**을 완성하는 웹 개발의 유기적인 흐름을 깊이 체감할 수 있었습니다. 경청해 주셔서 감사합니다!"


1. <header> — 머리말 영역
말한 것처럼 머리말이 맞습니다. 보통 페이지나 특정 구역의 시작 부분을 나타냅니다.
네 페이지에서는 다음 내용이 들어 있습니다.
- 상하농원·체험예약 등의 메뉴
- 로그인·회원가입 메뉴
- 상하농원 로고
- 농원소개·짓다·놀다 등의 메뉴
<header class="top-banner">
    ...
</header>
header라고 해서 반드시 페이지에 한 번만 써야 하는 것은 아닙니다. 네 코드처럼 상단 영역을 두 부분으로 나눠서 사용할 수도 있습니다.
2. <nav> — 이동 메뉴 영역
nav는 navigation의 줄임말입니다. 쉽게 말하면 사용자가 다른 페이지나 다른 위치로 이동할 때 사용하는 주요 메뉴 묶음입니다.
네 페이지에서는 이것들이 nav입니다.
<nav class="main-menu">
    <a href="#">상하농원</a>
    <a href="#">체험예약</a>
    <a href="#">객실예약</a>
</nav>
여기서 nav는 실제로 이동시키는 기능이 아니라 **“이 안에 이동용 메뉴들이 들어 있다”**라는 의미입니다. 실제 이동 주소는 a 태그의 href가 담당합니다.
현재는 href="#"이므로 이동할 주소가 아직 없는 상태입니다.
3. <section> — 주제가 있는 구역
section은 그냥 아무 구역이라기보다 하나의 주제나 내용을 가진 구역입니다.
네 페이지에서는 큰 배너 영역에 사용했습니다.
<section class="hero">
    ...
</section>
즉 “여기는 상하농원 소개 배너라는 하나의 내용 구역이다”라는 의미입니다.
단순히 디자인 때문에 요소들을 묶는다면 div를 사용하고, 제목이나 주제가 있는 콘텐츠 구역이라면 section을 사용할 수 있습니다.
4. <footer> — 페이지 하단 정보
footer는 페이지나 구역의 맨 아래에 들어가는 마무리 정보입니다.
네 페이지에서는 다음 내용이 들어 있습니다.
- 이용약관
- 개인정보취급방침
- 회사 주소
- 고객센터 전화번호
- 앱 다운로드 QR 코드
<footer class="footer">
    ...
</footer>
쉽게 말하면:
- header = 상단 소개
- nav = 이동 메뉴
- section = 주제가 있는 본문 구역
- footer = 하단 마무리 정보
5. box-sizing: border-box
웹페이지의 모든 요소는 네모난 상자라고 생각하면 됩니다.
* {
    box-sizing: border-box;
}
네 카드에는 다음과 같이 너비와 안쪽 여백이 있습니다.
.keyword-box {
    width: 320px;
    padding: 40px 20px;
    border: 1px solid #ddd;
}
border-box가 없으면:
- 내용 너비 320px
- 좌우 여백 40px
- 좌우 테두리 2px
- 실제 너비 362px
이렇게 설정한 크기보다 커집니다.
border-box를 사용하면 여백과 테두리까지 모두 포함해서 실제 너비가 320px로 유지됩니다.
발표에서는 이렇게 말하면 됩니다.
border-box를 사용해서 패딩과 테두리를 포함한 전체 크기가 지정한 너비를 넘지 않도록 했습니다.

6. position: relative와 absolute
네 배너에는 배경 이미지 위에 어두운 반투명 막이 올라가 있습니다.
.hero {
    position: relative;
}

.hero-overlay {
    position: absolute;
    inset: 0;
}
쉽게 비유하면:
- relative = “이 상자를 위치의 기준점으로 삼겠다.”
- absolute = “기준점 안에서 원하는 위치에 겹쳐놓겠다.”
hero에 position: relative를 지정해서 배너 전체를 기준으로 만들고, hero-overlay를 absolute로 설정해서 배너 위를 전부 덮은 것입니다.
inset: 0;
이것은 위·아래·왼쪽·오른쪽을 모두 0으로 지정해서 부모 영역 전체를 덮으라는 뜻입니다.
7. class="on"
<a href="#" class="on">상하농원은</a>
on은 HTML에 원래 정해진 기능이 아닙니다. 개발자가 직접 만든 클래스 이름입니다.
CSS에서는 다음처럼 on이 붙은 메뉴만 다르게 꾸몄습니다.
.sub-tab a.on {
    color: #4a5d1a;
    font-weight: bold;
    border-bottom: 2px solid #4a5d1a;
}
따라서 현재 선택된 ‘상하농원은’ 메뉴만 진한 색과 밑줄로 표시됩니다.
쉽게 말하면 on은 “현재 선택된 메뉴”라는 표시용 이름표입니다.
8. 이미지의 alt
<img src="images/logo.png" alt="상하농원 로고">
alt는 alternative text, 즉 대체 텍스트입니다.
다음과 같은 경우 사용됩니다.
- 이미지 파일을 불러오지 못했을 때
- 시각장애인이 화면 낭독기를 사용할 때
- 이미지가 무엇을 나타내는지 설명할 때
alt="상하농원 로고"는 “이 이미지는 상하농원 로고입니다”라는 뜻입니다.
9. flex-wrap: wrap
Flexbox는 기본적으로 자식 요소들을 한 줄에 넣으려고 합니다.
.keyword-wrap {
    display: flex;
    flex-wrap: wrap;
}
카드 세 개가 들어갈 공간이 부족할 때 flex-wrap: wrap이 있으면 다음 줄로 내려갑니다.
화면이 넓을 때:  [짓다] [놀다] [먹다]

화면이 좁을 때:  [짓다] [놀다]
                 [먹다]
즉, 요소가 찌그러지는 것을 줄이고 자연스럽게 줄바꿈해 주는 기능입니다.
교안에는 있지만 네 페이지에서는 중요하지 않은 것
Grid
Flexbox는 주로 한 방향으로 배치합니다.
[카드] [카드] [카드]
Grid는 가로와 세로를 함께 사용하는 바둑판 구조입니다.
[카드] [카드]
[카드] [카드]
네 페이지는 주로 가로 정렬이기 때문에 Flexbox를 사용했고, Grid는 사용하지 않았습니다.
Form
사용자가 정보를 입력하는 영역입니다.
<form>
    <input type="text">
    <button>로그인</button>
</form>
로그인 창, 검색창, 회원가입 입력란 등에 사용합니다. 네 페이지에는 실제 입력 기능이 없으므로 사용하지 않았습니다.
display: none
요소를 화면에서 완전히 숨기는 CSS입니다.
.menu {
    display: none;
}
요소가 보이지 않을 뿐만 아니라 차지하던 공간도 사라집니다. 네 페이지에서는 사용하지 않았기 때문에 발표할 필요가 없습니다.
결국 네 발표에서 꼭 이해할 것은 nav, Flexbox, 배경 이미지, 부모·자식 박스 구조 정도입니다. 나머지는 질문이 나왔을 때 설명할 수 있는 보충 내용으로만 알아두면 충분합니다.